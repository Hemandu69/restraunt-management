// Placeholder until a real restaurant-configuration/tables module exists
// (see root README "Tables are a core concept"). Table selection here is
// client-side only - nothing is persisted, and it resets on reload. No
// Available/Occupied status is shown because that data doesn't exist yet;
// showing it would be fabricated.
export const TABLE_COUNT = 12;
